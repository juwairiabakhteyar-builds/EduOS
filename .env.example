# EduOS environment configuration
# Copy these values into your local environment / Render Environment Variables.

DEBUG=True
DJANGO_SECRET_KEY=replace-with-a-long-random-secret
ALLOWED_HOSTS=localhost,127.0.0.1
